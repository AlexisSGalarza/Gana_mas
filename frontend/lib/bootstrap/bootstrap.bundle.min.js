/*
 * Bootstrap v5.3.2 - JavaScript Framework
 * Para descargar la versión completa, visita:
 * https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js
 * 
 * Este es un archivo placeholder. Para usar la aplicación:
 * 1. Descarga bootstrap.bundle.min.js del enlace de arriba
 * 2. Reemplaza este archivo con el descargado
 * 3. O usa el CDN cambiando la referencia en el HTML
 */

// Funcionalidades básicas de emergencia mientras descargas Bootstrap
console.log('⚠️ Bootstrap placeholder cargado');
console.log('📥 Descarga Bootstrap real desde: https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js');

// Polyfill básico para navbar toggle
document.addEventListener('DOMContentLoaded', function() {
    const toggleBtn = document.querySelector('.navbar-toggler');
    const navbarCollapse = document.querySelector('.navbar-collapse');
    
    if (toggleBtn && navbarCollapse) {
        toggleBtn.addEventListener('click', function() {
            navbarCollapse.classList.toggle('show');
        });
    }
});

// ⚠️ IMPORTANTE: Reemplaza este archivo con Bootstrap real para la funcionalidad completa
